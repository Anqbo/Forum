<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menu</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
        }

        .navi{
            display: flex;
            align-items: center;
            justify-content: space-between;
            background-color: #333;
            padding: 10px 20px;
        }

        .navi h1 {
            color: #fff;
            font-size: 24px;
        }

        .navi ul {
            display: flex;
            list-style-type: none;
            justify-content: center;
            padding: 10px 0;
        }

        .navi ul li {
            margin: 0 15px;
        }

        .navi ul li a {
            color: #fff;
            text-decoration: none;
            font-size: 16px;
            padding: 8px 12px;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        .navi ul li a:hover {
            background-color: #575757;
            border-radius: 4px;
        }
        .content {
            padding: 20px;
            text-align: center;
            background-color: #f4f4f4;
            margin: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            
        }
       input[type="text"] {
            padding: 6px 10px;
            border: 1px solid #ccc; 
            border-radius: 4px; 
            font-size: 14px; 
        }

        button {
            padding: 8px 16px; 
            background-color: #808000;; 
            color: #fff;
            border: none;
            border-radius: 4px; 
            cursor: pointer;
            font-size: 14px;
        }

         
        button:hover {
            background-color: #807030;
        }
 .bottom {
    display: flex;
    justify-content: space-between; /* Rozmieszcza dzieci równomiernie w poziomie */
    align-items: center; /* Wyśrodkowuje dzieci w pionie */
    text-align: center; /* Opcjonalnie, centruje tekst wewnątrz dzieci */
    margin-top: 20px; /* Dodatkowy odstęp od górnego bloku */
}

.bottom hr {
        width: 80%;
        margin: 20px auto; /* Centrowanie hr */
        border-color: #ccc;
}

.bottom .new,
.bottom .last {
    padding: 10px; /* Dodatkowy padding dla estetyki */
}

.bottom .new {
    margin-right: 10px; /* Odstęp między blokami new i last */
    width: calc(30% - 10px);
}

.bottom .last {
    margin-left: 10px; /* Odstęp między blokami new i last */
    width: calc(70% - 10px);
    border-left: 1px solid #ccc;
}

        #wiecej {
            text-decoration: none;
            color: black;
        }
        .new {
    background-color: #f0f0f0;
    padding: 20px;
    border: 1px solid #ccc;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    text-align: center; /* Opcjonalnie, jeśli chcesz tekst wewnątrz centrować */
}

.new h2 {
    font-size: 24px;
    margin-bottom: 10px;
}
    </style>
</head>
<body>
    <div class="navi">
        <h1>
        <form method='GET' action='/search'>
        <input type='text' name='wyszukiwarka'><button type='submit'>search</button>
        </form>
        </h1>
        <ul>
            <li><a href="/">Main Page</a></li>
            <li><a href="/wpisy">Posts</a></li>
            <li><a href="/about">About Me</a></li>
        </ul>
    </div>
    <div class="content">
        <div class="top">
            <h1>FORUM FOR 4</h1>
            <BR>
        </div>
        <div class="bottom">
            <div class="new">
                <h2> Add your own post</h2>
                <a href="/add"><button class="back">Add</button></a><br />
            </div>
            <div class="last">
                <h2> Last Post</h2>
                <p id="data">{{ wpis.dateAdded|date('Y-m-d') }}</p>
                <h3>{{wpis.title}}</h3>
                {# <h4>Kategria: {{wpis.category}}</h4> #}
                <p id="content">{{ wpis.content[:200]}}...<a id="wiecej" href="wpis/{{wpis.id}}"><b>more</b></a></p><br>
            </div>
        </div>
    </div>
</body>
</html>